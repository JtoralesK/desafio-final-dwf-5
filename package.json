{
  "name": "carpeta8-desafio-final",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build  --public-url /desafio-final-dwf-5",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@parcel/transformer-image": "2.0.0-rc.0",
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0-rc.0"
  }
}
